const c=document.querySelector("#c"),g=c.getContext("2d"),o=document.createElement("canvas"),x=o.getContext("2d"),sites=[];let w,h,d,S=9,m={x:-999,y:-999};
function fit(){w=innerWidth;h=innerHeight;d=Math.min(devicePixelRatio||1,2);c.width=w*d;c.height=h*d;g.setTransform(d,0,0,d,0,0);o.width=Math.ceil(w/S);o.height=Math.ceil(h/S);sites.length=0;for(let i=0;i<22;i++)sites.push({x:Math.random()*o.width,y:Math.random()*o.height,vx:(Math.random()-.5)*.05,vy:(Math.random()-.5)*.05,h:75+Math.random()*90})}addEventListener("resize",fit);addEventListener("pointermove",e=>{m.x=e.clientX/S;m.y=e.clientY/S});
function loop(){for(const s of sites){let dx=s.x-m.x,dy=s.y-m.y,r=Math.hypot(dx,dy)||1;if(r<16){s.vx+=dx/r*.025;s.vy+=dy/r*.025}s.x+=s.vx;s.y+=s.vy;if(s.x<0||s.x>o.width)s.vx*=-1;if(s.y<0||s.y>o.height)s.vy*=-1}let im=x.createImageData(o.width,o.height),a=im.data;for(let Y=0;Y<o.height;Y++)for(let X=0;X<o.width;X++){let a1=1e9,a2=1e9,hit;for(const s of sites){let r=(X-s.x)**2+(Y-s.y)**2;if(r<a1){a2=a1;a1=r;hit=s}else if(r<a2)a2=r}let edge=Math.min(1,(Math.sqrt(a2)-Math.sqrt(a1))*.65),i=(Y*o.width+X)*4;a[i]=25+hit.h*.25;a[i+1]=35+hit.h*.55;a[i+2]=25+hit.h*.18;a[i+3]=255-edge*170}x.putImageData(im,0,0);g.imageSmoothingEnabled=true;g.drawImage(o,0,0,w,h);requestAnimationFrame(loop)}fit();loop();
