const c=document.querySelector("#scene"),g=c.getContext("2d"),chars="01アイウエオカキクケコサシスセソ<>/{}[]",m={x:-999,y:-999,on:false};let w,h,d,size=17,drops=[];
function fit(){w=innerWidth;h=innerHeight;d=Math.min(devicePixelRatio||1,2);c.width=w*d;c.height=h*d;g.setTransform(d,0,0,d,0,0);drops=Array.from({length:Math.ceil(w/size)},()=>Math.random()*-60)}addEventListener("resize",fit);addEventListener("pointermove",e=>{m.x=e.clientX;m.y=e.clientY;m.on=true});document.documentElement.addEventListener("pointerleave",()=>m.on=false);
function loop(){g.fillStyle="rgba(1,8,6,.09)";g.fillRect(0,0,w,h);g.font=`${size-3}px monospace`;for(let i=0;i<drops.length;i++){const X=i*size,Y=drops[i]*size,dist=Math.hypot(X-m.x,Y-m.y);if(!m.on||dist>115){const ch=chars[Math.floor(Math.random()*chars.length)],a=Math.random()>.97?1:.25+Math.random()*.45;g.fillStyle=`rgba(78,240,160,${a})`;g.fillText(ch,X,Y);drops[i]+=.45+Math.random()*.12}else drops[i]+=(Y<m.y?-.22:.85);if(Y>h&&Math.random()>.975)drops[i]=Math.random()*-20}requestAnimationFrame(loop)}fit();loop();
