const c=document.querySelector("#scene"),g=c.getContext("2d"),s=[],m={x:innerWidth/2,y:innerHeight/2,down:false};let w,h,d,cx,cy,vel=7;
function reset(q,random=true){q.x=(Math.random()-.5)*w*1.7;q.y=(Math.random()-.5)*h*1.7;q.z=random?Math.random()*w:w;q.pz=q.z;return q}function size(){w=innerWidth;h=innerHeight;d=Math.min(devicePixelRatio||1,2);c.width=w*d;c.height=h*d;g.setTransform(d,0,0,d,0,0);cx=w/2;cy=h/2;while(s.length<700)s.push(reset({}));s.length=700}addEventListener("resize",size);addEventListener("pointermove",e=>{m.x=e.clientX;m.y=e.clientY});addEventListener("pointerdown",()=>m.down=true);addEventListener("pointerup",()=>m.down=false);
function loop(){g.fillStyle="rgba(2,5,12,.38)";g.fillRect(0,0,w,h);cx+=(m.x-cx)*.025;cy+=(m.y-cy)*.025;vel+=((m.down?25:7)-vel)*.04;for(const q of s){q.pz=q.z;q.z-=vel;if(q.z<1)reset(q,false);const k=220/q.z,pk=220/q.pz,x=q.x*k+cx,y=q.y*k+cy,px=q.x*pk+cx,py=q.y*pk+cy;if(x<0||x>w||y<0||y>h){reset(q,false);continue}const a=Math.min(1,1-q.z/w);g.beginPath();g.moveTo(px,py);g.lineTo(x,y);g.strokeStyle=`rgba(${170+a*85},${205+a*45},255,${.18+a*.82})`;g.lineWidth=.3+a*1.8;g.stroke()}requestAnimationFrame(loop)}size();loop();
